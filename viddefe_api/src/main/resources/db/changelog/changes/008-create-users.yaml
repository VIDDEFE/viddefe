databaseChangeLog:
  - changeSet:
      id: 008-create-users
      author: juan
      changes:
        - createTable:
            tableName: users
            columns:
              - column:
                  name: id
                  type: varchar(40)
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: passwordc
                  type: varchar(256)
                  constraints:
                    nullable: false
              - column:
                  name: rol_user_id
                  type: bigint
                  constraints:
                    nullable: false
        - addForeignKeyConstraint:
            baseTableName: users
            baseColumnNames: rol_user_id
            referencedTableName: roles_user
            referencedColumnNames: id
            constraintName: fk_rol_user_id